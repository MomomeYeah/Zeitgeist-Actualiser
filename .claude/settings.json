{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "shell": "bash",
            "timeout": 300,
            "statusMessage": "Checking lint, format, types, tests",
            "command": "uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pytest || { echo 'Definition of Done failed. Fix the failures above before completing this task.' >&2; exit 2; }"
          }
        ]
      }
    ]
  }
}
